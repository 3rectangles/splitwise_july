package com.example.splitwise_july.commands;

//attributes of an interface are static and final, and can be used throughout the code.
//enums are used to represent the types of entity, for constants we can use interface

public interface CommandKeywords {
    // good practice to use uppercase for constants
     public String REGISTER_USER = "Register";
    String UPDATE_PROFILE = "UpdateProfile";
//    double PI = 3.14; static final

}
